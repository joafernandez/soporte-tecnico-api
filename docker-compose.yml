version: "3.8"

services:
  mongodb:
    image: mongo:7
    container_name: mongo_mesa_ayuda
    ports:
    - "127.0.0.1:27017:27017"
    volumes:
      - mongo_data:/data/db
    restart: always

volumes:
  mongo_data:
   name: mongo_mesa_ayuda


